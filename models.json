{
  "_notes": "Runtime model selection is now delegated to @earendil-works/pi-ai built-in providers. Set BALAUR_MODEL=provider/model, and use the synthetic default via synthetic/syn:large:text unless another provider/model is configured."
}
