# Memory instructions

Keep this public tree free of private memory. Durable public examples may live in `memory/pages/`, but real AI/human memory should use external paths via `PI_MEMORY_*` environment variables.
